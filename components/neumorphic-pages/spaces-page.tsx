// Import the actual spaces page component
import SpacesPage from '@/app/spaces/page'

export function NeumorphicSpacesPage() {
  // Render the full-featured spaces page from the finance-screenshots branch
  return <SpacesPage />
}
